# إرسال الطلبات تلقائيًا عبر WhatsApp Cloud API

## 1. افتح نافذة المحادثة للاختبار

من رقم الاستقبال `+212631751974` أرسل رسالة مثل `سلام` إلى رقم Meta التجريبي الظاهر في صفحة **Faites un essai**. يسمح ذلك بإرسال رسالة نصية للاختبار داخل نافذة المحادثة.

## 2. إعداد Backend

افتح `backend/.env` وأضف القيم التالية في آخر الملف:

```env
WHATSAPP_ENABLED=true
WHATSAPP_ACCESS_TOKEN=PASTE_YOUR_NEW_PRIVATE_TOKEN_HERE
WHATSAPP_PHONE_NUMBER_ID=1235976616275400
WHATSAPP_RECIPIENT_NUMBER=212631751974
WHATSAPP_GRAPH_API_VERSION=v25.0
```

- استبدل `PASTE_YOUR_NEW_PRIVATE_TOKEN_HERE` بالـToken الجديد من Meta.
- لا ترسل الـToken لأي شخص، ولا تضعه في `frontend/.env`.
- الـToken التجريبي مؤقت؛ عند انتهاء صلاحيته سيتوقف الإرسال الآلي حتى تستبدله.

## 3. التشغيل والاختبار

أوقف المشروع ثم شغله من مجلد `fashion-fullstack`:

```powershell
npm run dev
```

أنشئ طلبًا تجريبيًا من المتجر. يحفظ الـBackend الطلب في PostgreSQL أولًا، ثم يرسل تفاصيله إلى واتساب. إذا فشل Cloud API، يبقى الطلب محفوظًا ويفتح الموقع رسالة واتساب اليدوية كخطة بديلة.

## 4. الإنتاج

الرسائل النصية الحرة تعمل داخل نافذة المحادثة. للتشغيل الدائم خارج هذه النافذة، يجب تسجيل رقم إرسال تجاري وإنشاء قالب رسالة معتمد في WhatsApp Manager والحصول على Token دائم. لا يلزم Webhook لإرسال الطلبات؛ نضيفه لاحقًا فقط عند الحاجة لاستقبال الرسائل أو حالات التسليم.
