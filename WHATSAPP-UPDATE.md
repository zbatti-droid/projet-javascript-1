# تحديث واتساب

## ضبط رقم المتجر

أنشئ ملفًا باسم `.env` داخل مجلد `frontend`، ثم أضف رقم واتساب بالصيغة الدولية بدون علامة `+` أو مسافات:

```env
VITE_WHATSAPP_NUMBER=212612345678
```

استبدل الرقم التجريبي برقم واتساب الخاص بالمتجر، ثم أوقف المشروع وشغّله من جديد:

```powershell
npm run dev
```

## ما الذي أضيف؟

- زر واتساب أخضر عائم يظهر في جميع صفحات المتجر.
- رسالة استفسار جاهزة عند الضغط على الزر.
- بعد نجاح الطلب وحفظه في PostgreSQL، يفتح واتساب تلقائيًا برسالة تتضمن رقم الطلب، بيانات الزبون، المنتجات، الكمية، المقاس، اللون، المبلغ والتوصيل.
- إذا لم يُضبط رقم واتساب، يبقى الطلب محفوظًا وتظهر رسالة تنبيه.
